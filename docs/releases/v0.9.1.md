# Release v0.9.1

## Fixes

- Fixed the GitHub release pipeline failing to locate the compiled binary after the amux → awman rename. All build matrix targets now reference the correct `awman` binary name and produce correctly named release artifacts.
